name: todo_fall_2024_0
description: "TODO Fall 2024"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.1
  firebase_core: ^3.6.0
  sign_in_button: ^3.2.0
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.4.4
  flutter_local_notifications: ^18.0.1
  app_settings: ^5.1.1
  timezone: ^0.10.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
